name: test
channels:
  - conda-forge

dependencies:
    # Base depends
  - python
  - pip
  - numpy
  - scipy
  - matplotlib
  - openmm

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  #- pip:
  #  - codecov

